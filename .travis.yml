language: go

go:
  - 1.4

before_install:
  - export PROJECT_HOME=$HOME/gopath/src/hypermind.cn/talon
  - mkdir -p $PROJECT_HOME
  - export TRAVIS_BUILD_DIR=$PROJECT_HOME
  - cp -R $HOME/gopath/src/github.com/hyper-carrot/talon $PROJECT_HOME
  - cd $PROJECT_HOME
  - echo $PROJECT_HOME

install:
  - go get github.com/PuerkitoBio/goquery

before_script:
  - chmod +x ./test

script:
  - ./test
